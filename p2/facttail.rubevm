fib:
    const r1, 1
    const r2, 1 
    if_zero r0, 6
    mul r1, r0, r1 
    sub r0, r0, r2 
    jmp 2
    ret r1

main:
    const r0, print_int
    const r1, fib
    const r2, 20
    call r1, r2, r2
    call r0, r2, r2
    halt r2