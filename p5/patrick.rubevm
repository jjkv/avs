main:
	rd_glob r0, a
	rd_glob r1, b
	const r2, h2
	const r3, h3
	if_zero r0, 1
	if_zero r1, 1
	ret r2
	ret r3

harness:
	rd_glob r0, a
	rd_glob r1, b
	const r2, 2
	const r3, 3
	if_zero r0, 1
	if_zero r1, 1
	ret r2
	ret r3