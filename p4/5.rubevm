main:
	int_sym r0, a
	if_zero r0, 1
	# 1st path, a != 0, fail
	assert_zero r0
	int_sym r0, b
	if_zero r0, 1
	# 2nd path, b != 0, fail
	assert_zero r0
	int_sym r0, c
	if_zero r0, 2
	const r3, 3
	# 3rd path, c != 0, return 3
	ret r3
	int_sym r0, d
	if_zero r0, 2
	const r4, 4
	# 4th path, d != 0, return 4
	ret r4
	const r5, 5
	#5th path, d == 0, return 5
	ret r5
